// InteractiveToolbar is now handled entirely inside the iframe via the
// injected runtime in InteractiveOverlay.tsx.
// This file is kept as a no-op stub to avoid breaking any existing imports.

export default function InteractiveToolbar() {
  return null;
}